<main class="max-w-5xl w-full mx-auto py-8">
  <h1 class="text-center text-2xl font-semibold">
    Create a note for <%= @patient.name %>
  </h1>

  <div class="max-w-lg w-full mx-auto mt-8">
    <%= render "form", patient: @patient, note: @note %>
  </div>
</main>
